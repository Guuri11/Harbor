// AUTO-GENERATED by puerto. Edit puerto.toml, then run `puerto generate bootstrap`.
use std::sync::Arc;

{% if entities -%}
{% for entity in entities -%}
{% for line in entity.uc_import_lines -%}
{{ line }}
{% endfor -%}
{% endfor %}
{% for entity in entities -%}
{{ entity.repo_trait_import }}
{{ entity.repo_impl_import }}
{% endfor -%}
use infrastructure::logger::TracingLogger;
use business::domain::logger::LoggerTrait;

use poem::{EndpointExt, Route, middleware::{Cors, Tracing}};
use poem_openapi::OpenApiService;

{% for entity in entities -%}
{{ entity.api_import }}
{% endfor %}
pub async fn build_app() -> impl poem::Endpoint {
{% if has_db -%}
    let database_url = std::env::var("DATABASE_URL").expect("DATABASE_URL must be set");
    let pool = infrastructure::db::create_postgres_pool(&database_url)
        .await
        .expect("Failed to connect to database");
    infrastructure::db::run_migrations(&pool)
        .await
        .expect("Failed to run migrations");

{% endif -%}
    let logger: Arc<dyn LoggerTrait> = Arc::new(TracingLogger);

{% for entity in entities -%}
{% if entity.needs_repo_var -%}
{{ entity.repo_var_line }}
{% endif -%}
{% for uc in entity.use_cases -%}
    let {{ uc.action }} = Arc::new({{ uc.uc_pascal }}UseCaseImpl { repository: {{ uc.repo_expr }}, logger: Arc::clone(&logger) });
{% endfor -%}
    let {{ entity.api_var }} = {{ entity.pascal }}Api { {{ entity.use_case_fields }}, logger: Arc::clone(&logger) };

{% endfor -%}
    let port = std::env::var("SERVICE_PORT").unwrap_or_else(|_| "8080".to_string());
    let api_service = OpenApiService::new(
        {{ api_args }},
        env!("CARGO_PKG_NAME"),
        env!("CARGO_PKG_VERSION"),
    )
    .server(format!("http://localhost:{port}/api"));
    let ui = api_service.swagger_ui();

    let cors_origins = std::env::var("CORS_ALLOWED_ORIGINS").unwrap_or_default();
    let mut cors = Cors::new();
    for origin in cors_origins.split(',').map(str::trim).filter(|s| !s.is_empty()) {
        cors = cors.allow_origin(origin);
    }

    Route::new().nest("/api", api_service).nest("/", ui).with(Tracing).with(cors)
}
{% else -%}
use infrastructure::logger::TracingLogger;
use business::domain::logger::LoggerTrait;

use poem::{EndpointExt, Route, middleware::{Cors, Tracing}};
use poem_openapi::OpenApiService;

pub async fn build_app() -> impl poem::Endpoint {
    let _logger: Arc<dyn LoggerTrait> = Arc::new(TracingLogger);

    let port = std::env::var("SERVICE_PORT").unwrap_or_else(|_| "8080".to_string());
    let api_service = OpenApiService::new(
        (),
        env!("CARGO_PKG_NAME"),
        env!("CARGO_PKG_VERSION"),
    )
    .server(format!("http://localhost:{port}/api"));
    let ui = api_service.swagger_ui();

    let cors_origins = std::env::var("CORS_ALLOWED_ORIGINS").unwrap_or_default();
    let mut cors = Cors::new();
    for origin in cors_origins.split(',').map(str::trim).filter(|s| !s.is_empty()) {
        cors = cors.allow_origin(origin);
    }

    Route::new().nest("/api", api_service).nest("/", ui).with(Tracing).with(cors)
}
{% endif -%}
